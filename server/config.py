import pytz

# the interval during which the CPU load is measured (seconds)
CPU_SAMPLE_TIME = 1

# interval for writing values to the database (seconds)
JOB_CPU_INTERVAL_TIME = 5

TIME_ZONE = pytz.timezone('Europe/Moscow')
